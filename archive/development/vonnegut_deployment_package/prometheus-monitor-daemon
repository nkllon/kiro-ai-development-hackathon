#!/usr/bin/env python3
"""
Prometheus Monitor Daemon Control Script
========================================

Command-line interface for managing the Prometheus monitoring daemon.
Provides standard service management commands: start, stop, restart, status.

Usage:
    prometheus-monitor-daemon --start
    prometheus-monitor-daemon --stop
    prometheus-monitor-daemon --restart
    prometheus-monitor-daemon --status
    prometheus-monitor-daemon --health-check

Author: Beast Mode Framework
Date: 2025-09-19
Purpose: Service management for monitoring daemon
"""

import sys
import os
import argparse
from pathlib import Path

# Add src to Python path
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))

from beast_mode.monitoring.daemon import main

if __name__ == "__main__":
    main()