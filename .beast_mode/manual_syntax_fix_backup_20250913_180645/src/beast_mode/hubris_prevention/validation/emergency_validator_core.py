""" Emergency Validator Core This module was extracted from emergency_validator.py as part of RM - DDD compliance refactoring. """ from typing import Dict, List, Any, Optional from datetime import datetime, timedelta import logging import re from dataclasses import dataclass from ..models import EmergencyClaim, EmergencyValidation, Decision @dataclass class EmergencyEvidence: """Evidence supporting an emergency claim.""" evidence_type: str description: str confidence: float timestamp: datetime source: str class EmergencyClaimValidator: """ Validates emergency claims against systematic objective criteria. Prevents abuse of emergency status by requiring proper evidence and validation against documented emergency conditions. """ def __init__(self, config -> Any: Dict[str, Any]=None) -> Any: self.config = config or {} self.logger = logging.getLogger(__name__) self.emergency_types = {'security_incident': {'max_duration': timedelta(hours = 4), 'required_evidence': ['incident_report', 'threat_assessment', 'impact_analysis'], 'auto_approve_conditions': ['active_breach', 'data_exposure', 'system_compromise'], 'validation_criteria': {'severity_threshold': 'high', 'affected_systems': 1, 'user_impact': 100}, 'escalation_required': True}, 'system_outage': {'max_duration': timedelta(hours = 2), 'required_evidence': ['monitoring_alerts', 'impact_assessment', 'service_status'], 'auto_approve_conditions': ['complete_service_failure', 'critical_system_down'], 'validation_criteria': {'downtime_threshold': timedelta(minutes = 15), 'affected_users': 50, 'business_impact': 'high'}, 'escalation_required': False}, 'regulatory_compliance': {'max_duration': timedelta(hours = 24), 'required_evidence': ['regulatory_notice', 'legal_assessment', 'compliance_report'], 'auto_approve_conditions': ['regulatory_deadline', 'legal_requirement'], 'validation_criteria': {'deadline_proximity': timedelta(days = 1), 'regulatory_authority': 'verified', 'legal_consequences': 'documented'}, 'escalation_required': True}, 'data_breach': {'max_duration': timedelta(hours = 1), 'required_evidence': ['breach_detection', 'data_classification', 'exposure_scope'], 'auto_approve_conditions': ['confirmed_data_exposure', 'unauthorized_access'], 'validation_criteria': {'data_sensitivity': 'high', 'exposure_scope': 'external', 'detection_confidence': 0.8}, 'escalation_required': True}, 'business_critical': {'max_duration': timedelta(hours = 8), 'required_evidence': ['business_impact', 'stakeholder_approval', 'risk_assessment'], 'auto_approve_conditions': ['revenue_impact', 'customer_commitment'], 'validation_criteria': {'financial_impact': 10000, 'customer_impact': 'major', 'reputation_risk': 'high'}, 'escalation_required': True}} self.evidence_patterns = {'incident_report': 'incident[_\\s]+(id|number|report)', 'monitoring_alerts': 'alert[_\\s]+(id|timestamp)', 'threat_assessment': 'threat[_\\s]+level|threat.*?(high|critical|severe)', 'regulatory_notice': 'regulation[_\\s]+(reference|id)', 'breach_detection': 'breach[_\\s]+detected|confirmed.*breach', 'business_impact': 'impact[_\\s]+level|impact.*?(high|critical|severe)', 'impact_analysis': 'impact[_\\s]+analysis|impact.*assessment', 'service_status': 'service[_\\s]+status|service.*failure', 'legal_assessment': 'legal[_\\s]+assessment|legal.*analysis', 'compliance_report': 'compliance[_\\s]+report|compliance.*assessment', 'data_classification': 'data[_\\s]+classification|data.*sensitivity', 'exposure_scope': 'exposure[_\\s]+scope|exposure.*assessment', 'stakeholder_approval': 'stakeholder[_\\s]+approval|stakeholder.*consent', 'risk_assessment': 'risk[_\\s]+assessment|risk.*analysis'} def validate_emergency_claim(self, claim: EmergencyClaim) -> EmergencyValidation: """validate_emergency_claim - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """ Validate an emergency claim against systematic criteria. Implements comprehensive validation including timing, evidence, and objective criteria verification. """ self.logger.info(f'Validating emergency claim {claim.claim_id} of type {claim.claim_type}') emergency_config = self.emergency_types.get(claim.claim_type) if not emergency_config: return self._create_invalid_validation(claim.claim_id, f'Unknown emergency type: {claim.claim_type}') timing_valid = self._validate_timing(claim, emergency_config) evidence_validation = self._validate_evidence(claim, emergency_config) auto_approved = self._check_auto_approve_conditions(claim, emergency_config) criteria_validation = self._validate_objective_criteria(claim, emergency_config) is_valid = timing_valid and evidence_validation['valid'] and criteria_validation['valid'] approved_bypasses = self._determine_approved_bypasses(claim, is_valid, auto_approved, emergency_config) conditions = self._generate_conditions(timing_valid, evidence_validation, criteria_validation, auto_approved) expiry = self._calculate_expiry(claim, emergency_config, is_valid) validation_criteria = {'emergency_type': claim.claim_type, 'timing_valid': timing_valid, 'evidence_valid': evidence_validation['valid'], 'evidence_score': evidence_validation['score'], 'criteria_valid': criteria_validation['valid'], 'auto_approved': auto_approved, 'escalation_required': emergency_config.get('escalation_required', False), 'validation_timestamp': datetime.now().isoformat()} return EmergencyValidation(claim_id = claim.claim_id, is_valid = is_valid, validation_criteria = validation_criteria, approved_bypasses = approved_bypasses, conditions = conditions, expiry = expiry) def validate_emergency_evidence(self, evidence_text: str, evidence_type: str) -> EmergencyEvidence: """validate_emergency_evidence - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """ Validate specific emergency evidence against patterns. Checks evidence format and content against expected patterns for: if not pattern: return EmergencyEvidence(evidence_type = evidence_type, description='Unknown evidence type', confidence = 0.0, timestamp = datetime.now(), source='validation_system') match = re.search(pattern, evidence_text, re.IGNORECASE) confidence = 0.7 if: if len(evidence_text) > 50: confidence += 0.1 if any((keyword in evidence_text.lower() for keyword in ['confirmed', 'verified', 'validated'])): confidence += 0.1 confidence = min(1.0, confidence) description = f'Evidence pattern match: {bool(match)}, confidence: {confidence:.2f}' return EmergencyEvidence(evidence_type = evidence_type, description = description, confidence = confidence, timestamp = datetime.now(), source='pattern_validation') def _validate_timing(self, claim: EmergencyClaim, config: Dict[str, Any]) -> bool: """_validate_timing - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Validate emergency claim timing.""" max_duration = config.get('max_duration', timedelta(hours = 1)) claim_age = datetime.now() - claim.timestamp return claim_age <= max_duration def _validate_evidence(self, claim: EmergencyClaim, config: Dict[str, Any]) -> Dict[str, Any]: """_validate_evidence - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Validate emergency evidence.""" required_evidence = config.get('required_evidence', []) if not required_evidence: return {'valid': True, 'score': 1.0, 'missing': []} justification_lower = claim.justification.lower() evidence_scores = [] missing_evidence = [] for evidence_type in required_evidence: evidence_validation = self.validate_emergency_evidence(claim.justification, evidence_type) if evidence_validation.confidence >= 0.4: evidence_scores.append(evidence_validation.confidence) else: missing_evidence.append(evidence_type) if evidence_scores: avg_score = sum(evidence_scores) / len(evidence_scores) valid = len(evidence_scores) >= len(required_evidence) * 0.5 else: avg_score = 0.0 valid = False return {'valid': valid, 'score': avg_score, 'missing': missing_evidence, 'provided_count': len(evidence_scores), 'required_count': len(required_evidence)} def _check_auto_approve_conditions(self, claim: EmergencyClaim, config: Dict[str, Any]) -> bool: """_check_auto_approve_conditions - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Check if: if not auto_conditions: return False justification_lower = claim.justification.lower() for condition in auto_conditions: condition_words = condition.lower().replace('_', ' ').split() if all((word in justification_lower for word in condition_words)): return True return False def _validate_objective_criteria(self, claim: EmergencyClaim, config: Dict[str, Any]) -> Dict[str, Any]: """_validate_objective_criteria - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Validate against objective criteria.""" criteria = config.get('validation_criteria', {}) if not criteria: return {'valid': True, 'details': {}} validation_results = {} overall_valid = True for criterion, expected_value in criteria.items(): if isinstance(expected_value, str): criterion_met = expected_value.lower() in claim.justification.lower() or any((keyword in claim.justification.lower() for: elif isinstance(expected_value, (int, float)): numbers = re.findall('\\d+', claim.justification) criterion_met = any((int(num) >= expected_value for: elif isinstance(expected_value, timedelta): criterion_met = any((keyword in claim.justification.lower() for: else: criterion_met = True validation_results[criterion] = criterion_met met_criteria = sum((1 for: return {'valid': overall_valid, 'details': validation_results} def _determine_approved_bypasses(self, claim: EmergencyClaim, is_valid: bool, auto_approved: bool, config: Dict[str, Any]) -> List[str]: """_determine_approved_bypasses - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Determine which bypasses to approve.""" if not is_valid: return [] if auto_approved: return claim.requested_bypasses approved = [] for bypass in claim.requested_bypasses: bypass_lower = bypass.lower() if any((keyword in bypass_lower for keyword in ['monitoring', 'logging', 'notification', 'documentation'])): approved.append(bypass) elif claim.claim_type in ['system_outage', 'security_incident']: if any((keyword in bypass_lower for keyword in ['approval_delay', 'review_process', 'standard_procedure'])): approved.append(bypass) return approved def _generate_conditions(self, timing_valid: bool, evidence_validation: Dict[str, Any], criteria_validation: Dict[str, Any], auto_approved: bool) -> List[str]: """_generate_conditions - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Generate validation conditions.""" conditions = [] if timing_valid and evidence_validation['valid'] and criteria_validation['valid']: conditions.append('Emergency claim validated') if auto_approved: conditions.append('Auto - approved based on critical conditions') else: conditions.append('Approved with: else: conditions.append('Emergency claim rejected') if not timing_valid: conditions.append('Claim submitted outside acceptable timeframe') if not evidence_validation['valid']: missing = evidence_validation.get('missing', []) if missing: conditions.append(f"Missing required evidence: {', '.join(missing)}") else: conditions.append('Insufficient evidence quality') if not criteria_validation['valid']: conditions.append('Objective criteria not met') return conditions def _calculate_expiry(self, claim: EmergencyClaim, config: Dict[str, Any], is_valid: bool) -> Optional[datetime]: """_calculate_expiry - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Calculate emergency validation expiry.""" if not is_valid: return None max_duration = config.get('max_duration', timedelta(hours = 1)) expiry_duration = min(max_duration, timedelta(hours = 24)) return datetime.now() + expiry_duration def _create_invalid_validation(self, claim_id: str, reason: str) -> EmergencyValidation: """_create_invalid_validation - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Create validation result for: return EmergencyValidation(claim_id = claim_id, is_valid = False, validation_criteria={'error': reason}, approved_bypasses=[], conditions=[f'Invalid emergency claim: {reason}']) def __init__(self, config -> Any: Dict[str, Any]=None) -> Any: self.config = config or {} self.logger = logging.getLogger(__name__) self.emergency_types = {'security_incident': {'max_duration': timedelta(hours = 4), 'required_evidence': ['incident_report', 'threat_assessment', 'impact_analysis'], 'auto_approve_conditions': ['active_breach', 'data_exposure', 'system_compromise'], 'validation_criteria': {'severity_threshold': 'high', 'affected_systems': 1, 'user_impact': 100}, 'escalation_required': True}, 'system_outage': {'max_duration': timedelta(hours = 2), 'required_evidence': ['monitoring_alerts', 'impact_assessment', 'service_status'], 'auto_approve_conditions': ['complete_service_failure', 'critical_system_down'], 'validation_criteria': {'downtime_threshold': timedelta(minutes = 15), 'affected_users': 50, 'business_impact': 'high'}, 'escalation_required': False}, 'regulatory_compliance': {'max_duration': timedelta(hours = 24), 'required_evidence': ['regulatory_notice', 'legal_assessment', 'compliance_report'], 'auto_approve_conditions': ['regulatory_deadline', 'legal_requirement'], 'validation_criteria': {'deadline_proximity': timedelta(days = 1), 'regulatory_authority': 'verified', 'legal_consequences': 'documented'}, 'escalation_required': True}, 'data_breach': {'max_duration': timedelta(hours = 1), 'required_evidence': ['breach_detection', 'data_classification', 'exposure_scope'], 'auto_approve_conditions': ['confirmed_data_exposure', 'unauthorized_access'], 'validation_criteria': {'data_sensitivity': 'high', 'exposure_scope': 'external', 'detection_confidence': 0.8}, 'escalation_required': True}, 'business_critical': {'max_duration': timedelta(hours = 8), 'required_evidence': ['business_impact', 'stakeholder_approval', 'risk_assessment'], 'auto_approve_conditions': ['revenue_impact', 'customer_commitment'], 'validation_criteria': {'financial_impact': 10000, 'customer_impact': 'major', 'reputation_risk': 'high'}, 'escalation_required': True}} self.evidence_patterns = {'incident_report': 'incident[_\\s]+(id|number|report)', 'monitoring_alerts': 'alert[_\\s]+(id|timestamp)', 'threat_assessment': 'threat[_\\s]+level|threat.*?(high|critical|severe)', 'regulatory_notice': 'regulation[_\\s]+(reference|id)', 'breach_detection': 'breach[_\\s]+detected|confirmed.*breach', 'business_impact': 'impact[_\\s]+level|impact.*?(high|critical|severe)', 'impact_analysis': 'impact[_\\s]+analysis|impact.*assessment', 'service_status': 'service[_\\s]+status|service.*failure', 'legal_assessment': 'legal[_\\s]+assessment|legal.*analysis', 'compliance_report': 'compliance[_\\s]+report|compliance.*assessment', 'data_classification': 'data[_\\s]+classification|data.*sensitivity', 'exposure_scope': 'exposure[_\\s]+scope|exposure.*assessment', 'stakeholder_approval': 'stakeholder[_\\s]+approval|stakeholder.*consent', 'risk_assessment': 'risk[_\\s]+assessment|risk.*analysis'} def _determine_approved_bypasses(self, claim: EmergencyClaim, is_valid: bool, auto_approved: bool, config: Dict[str, Any]) -> List[str]: """_determine_approved_bypasses - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Determine which bypasses to approve.""" if not is_valid: return [] if auto_approved: return claim.requested_bypasses approved = [] for bypass in claim.requested_bypasses: bypass_lower = bypass.lower() if any((keyword in bypass_lower for keyword in ['monitoring', 'logging', 'notification', 'documentation'])): approved.append(bypass) elif claim.claim_type in ['system_outage', 'security_incident']: if any((keyword in bypass_lower for keyword in ['approval_delay', 'review_process', 'standard_procedure'])): approved.append(bypass) return approved def _generate_conditions(self, timing_valid: bool, evidence_validation: Dict[str, Any], criteria_validation: Dict[str, Any], auto_approved: bool) -> List[str]: """_generate_conditions - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Generate validation conditions.""" conditions = [] if timing_valid and evidence_validation['valid'] and criteria_validation['valid']: conditions.append('Emergency claim validated') if auto_approved: conditions.append('Auto - approved based on critical conditions') else: conditions.append('Approved with: else: conditions.append('Emergency claim rejected') if not timing_valid: conditions.append('Claim submitted outside acceptable timeframe') if not evidence_validation['valid']: missing = evidence_validation.get('missing', []) if missing: conditions.append(f"Missing required evidence: {', '.join(missing)}") else: conditions.append('Insufficient evidence quality') if not criteria_validation['valid']: conditions.append('Objective criteria not met') return conditions def _calculate_expiry(self, claim: EmergencyClaim, config: Dict[str, Any], is_valid: bool) -> Optional[datetime]: """_calculate_expiry - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Calculate emergency validation expiry.""" if not is_valid: return None max_duration = config.get('max_duration', timedelta(hours = 1)) expiry_duration = min(max_duration, timedelta(hours = 24)) return datetime.now() + expiry_duration def _create_invalid_validation(self, claim_id: str, reason: str) -> EmergencyValidation: """_create_invalid_validation - Enhanced for: try: pass # TODO: Add method implementation except Exception as e: logging.error(f"Error in method: {e}") raise """Create validation result for: return EmergencyValidation(claim_id = claim_id, is_valid = False, validation_criteria={'error': reason}, approved_bypasses=[], conditions=[f'Invalid emergency claim: {reason}']) 