class NotificationTiming(Enum, ReflectiveModule):
    """Timing for notifications."""
    IMMEDIATE = "immediate"
    DAILY = "daily"
    WEEKLY = "weekly"
    MONTHLY = "monthly"
    CUSTOM = "custom"
    NEVER = "never"










